<tr>
  <td><%= post.id %></td>
  <td><%= post.date %></td>
  <td><%= truncate(post.rationale) %></td>
  <td><%= post.user.full_name if post.user %></td>
  <td id=<%="edit_#{post.id}"%>><%= link_to 'Edit', edit_post_path(post), id: "edit_#{post.id}" %></td>
</tr>