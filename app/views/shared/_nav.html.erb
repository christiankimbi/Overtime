<div class="logo">
  <h1>Time Tracker</h1>
</div>

<ul class="nav nav-tabs custom-nav">
  <li class="nav-item">
    <%= link_to "", root_path, class: "nav-link #{active?(root_path)} glyphicon glyphicon-home" %>
  </li>
  <% if current_user %>
  <li class="nav-item">
    <%= link_to "Entries", posts_path, class: "nav-link #{active?(posts_path)}" %>
  </li>
    <!-- TODO: Possibly refactor " -->
  <%= render 'shared/audit_log_tab' if policy(AuditLog).index? %>
  <li class="nav-item">
    <%= link_to "Request Overtime", new_post_path, class: "nav-link #{active?(new_post_path)}", id: "new_post_from_nav" %>
  </li>
<% end %>
  <li class="dropdown float-right <%#= active?(edit_user_registration_path)%> ">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Account<span class="caret"></span></a>
    <ul class="dropdown-menu">
      <!--<li><%#= link_to "Edit Details",  edit_user_registration_path %></li> -->
      <% if current_user.try(:type) =='AdminUser' %>
           <li>
        <%= link_to "Admin Dashboard", admin_root_path %>
      </li>
        <% end %>
      <li role="separator" class="divider"></li>
      <% if current_user %>
        <li><%= link_to "Log out",  destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li><%= link_to "Log In",  new_user_session_path %></li>
      <% end %>

    </ul>
  </li>
</ul>

